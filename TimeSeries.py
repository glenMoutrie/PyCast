import numpy as np

class TimeSeries:

	def _init_(self, series, dates):
		self.series = series
		self.dates = dates
		clean = False

	def _init_(self, series):
		self.series = series
		self.dates = None
		clean = False


	series
	dates
	clean

	# TODO complete interpolation structure
	def cleanSeries():
		self.clean = True